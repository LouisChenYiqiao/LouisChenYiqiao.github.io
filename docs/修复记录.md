# 修复记录

> 工作区：`/Users/louisqiao/Desktop/Agent`

## 日期：2026.9.13

### 复查与优化

- 线上首页在桌面端可正常访问，导航、锚点和联系邮箱均有效。
- 调整首屏背景的光影叠层和对焦位置；滚动后导航栏增加轻微阴影，提升层次感且不改变原有暖金风格。
- 将个人信息由视觉列表改为语义化的定义列表；补齐主内容跳转的焦点目标，并保留键盘焦点与减少动态效果支持。
- CSP 收紧为仅加载本站资源，并显式禁止外部连接、媒体、对象和 Worker；删除无法由 `<meta>` 可靠强制的策略指令，避免形成虚假的安全保障。
- 新增 `.well-known/security.txt` 作为安全问题联系入口，并使用 `.nojekyll` 确保该标准路径会被 GitHub Pages 原样发布。
- 部署脚本改为经临时 `GIT_ASKPASS` 传递 PAT，令牌不再出现在 Git 远端 URL 或进程参数中；保留远端提交祖先检查，防止误覆盖线上更新。

### 验证

- `git diff --check`：通过。
- `bash -n scripts/deploy.sh`：通过。
- 内容安全策略与 HTML/JS 引用路径：人工复核通过。

## 日期：2026.8.24

### 背景

个人主页 `https://louischenyiqiao.github.io` 长期未维护，无法打开（返回 404）。

### 诊断结论

网站源码（GitHub 仓库 `LouisChenYiqiao/LouisChenYiqiao.github.io`）内容完好，共有 54 个文件，
是 Mobirise Website Builder v5.8.4 生成的项目。打不开的**根本原因**是 **GitHub Pages 未启用**（`has_pages: false`）。

### 处理过程

1. 克隆仓库到 Agent 工作区，备份原始 `index.html` / `project.mobirise` 到 `_archive_original/`。
2. 清理所有旧构建器痕迹（模板图片、模板库文件、`project.mobirise`）。
3. 重写 `index.html` + `assets/css/style.css`（纯手写、响应式、无构建工具）。
4. 建立维护文档 `docs/维护说明.md`、`README.md`、`.gitignore`。
5. 提交并推送到 GitHub `main` 分支。
6. 通过 GitHub API 启用 GitHub Pages（Source = `main` 分支根目录）。
7. 验证线上返回 200，且无旧构建器痕迹。

### 结果

✅ 网站已恢复上线：<https://louischenyiqiao.github.io>

### 凭证

- 使用经典 PAT（`repo` 权限）完成 push 与 Pages 操作。
- 该 PAT 未写入任何 git 配置文件或仓库文件，仅用于本地单次操作。

### 后续工作（凭据管理）

- 将 PAT 统一存放到 Agent 工作区根目录的 `secrets/github.token`（权限 600）。
- 新增 `scripts/deploy.sh` 部署脚本，后续更新只需执行 `./scripts/deploy.sh "说明"`。
- 通过 .gitignore 排除 `secrets/`、`*.token` 等，确保凭据不会被上传。
- 结论：免费账户无法在 Private 仓库上启用 Pages，因此仓库保持 Public。

### 内容更新

- 联系方式仅保留工作邮箱 `cyq_ustc@mail.ustc.edu.cn`。
- 联系方式区块改为单卡片居中布局（新增 `contact-grid--single` 样式）。
- 首次通过 `scripts/deploy.sh` 完整跑通「修改 → 提交 → 推送」流程，验证无误。

### 大幅改版（同日）

- 全站更名：统一替换为网名 `LouisQiao`（域名与仓库名不变）。
- 删除「我的工作」模块及其相关样式/配图；「关于我」区块改为纯文字居中布局。
- 美术风格重构：根据首页逆光剪影照片（黄金时刻暖金色调）重设全站配色，favicon 改为暖金「L」字。

### 提示

- 建议妥善保管 PAT，若担心泄露可随时在 GitHub 撤销并重新生成。
